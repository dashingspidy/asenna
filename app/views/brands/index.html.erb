<div class="grid grid-cols-2 gap-4 p-4">
  <section class="max-w-70 mt-4">
    <h3 class="text-xl mb-2">Add new brand/company</h3>
    <%= render "form", brand: @brand %>
  </section>
  <section>
    <div class="overflow-x-auto">
      <table class="table">
        <thead>
          <tr>
            <th>Brand Name</th>
          </tr>
        </thead>
          <tbody>
            <% @brands.each do %>
              <tr>
                <td><%= it.name %></td>
              </tr>
            <% end %>
          </tbody>
      </table>
    </div>
  </section>
</div>
