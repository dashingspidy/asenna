<section class="flex justify-center items-center h-screen">
  <fieldset class="fieldset w-xs bg-base-200 border border-base-300 p-4 rounded-box">
    <legend class="fieldset-legend">Forgot your password?</legend>
    
    <%= form_with url: passwords_path do |form| %>
      <%= form.label :email_address, class: "fieldset-label" %>
      <%= form.email_field :email_address, placeholder: "Email address", class: "input" %>

      <%= form.submit "Email reset instructions", class: "btn btn-neutral mt-4" %>
    <% end %>
  </fieldset>
</section>